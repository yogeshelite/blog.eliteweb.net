@model IEnumerable<EliteWeb.Models.Career>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var index = 0;
}
<div class="card card-body">
    <table class="table table-hover table-light">
        <tr class="">
            <th>Sno</th>
            <th>
                Job Title
            </th>
            <th>
                Location
            </th>
            <th>Action</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>@(++index)</td>
                <td>
                    @Html.DisplayFor(modelItem => item.jobTitle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.location)
                </td>

                @*<td>
                        @Html.DisplayFor(modelItem => item.ShortDiscription)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.jobintroduction)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Yourmission)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Skills)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BanerImage)
                    </td>*@
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                    @*@Html.ActionLink("Details", "Details", new { id = item.id }) |*@
                    @Html.ActionLink("Delete", "Delete", new { id = item.id })
                </td>
            </tr>

        }

    </table>
</div>
